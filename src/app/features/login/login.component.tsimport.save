import { FormBuilder, ReactiveFormsModule, FormGroup } from '@angular/forms';
import { Router } from '@angular/router';
import { NgIf } from '@angular/common';
import { AuthService } from '../../core/auth';

@Component({
  selector: 'app-login',
  standalone: true,
  imports: [ReactiveFormsModule, NgIf],
  template: `
  <div class="login-page" style="min-height: calc(100vh - 4rem); display:flex; align-items:center;">
    <div class="card card-outline card-success" style="max-width:560px; margin: 0 auto;">
      <div class="card-header"><h3 class="card-title">Acceso</h3></div>
      <div class="card-body">
        <form [formGroup]="f" (ngSubmit)="entrar()">
          <div class="form-group">
            <label for="usuario">Usuario</label>
            <input id="usuario" class="form-control" formControlName="user" placeholder="David Vargas R.">
          </div>
          <div class="form-group">
            <label for="password">Contraseña</label>
            <input id="password" type="password" class="form-control" formControlName="pass" placeholder="1234">
          </div>
          <div class="d-flex justify-content-end">
            <button type="button" class="btn btn-outline-success mr-2" (click)="guardar()">Guardar</button>
            <button type="submit" class="btn btn-success">Entrar</button>
          </div>
          <small class="text-muted d-block mt-2">
            Tip: “Guardar” almacena usuario/contraseña en localStorage.
          </small>
          <div *ngIf="err" class="alert alert-danger mt-3 mb-0">Credenciales inválidas.</div>
        </form>
      </div>
    </div>
  </div>`
})
export class LoginComponent {
  err = false;
  f!: FormGroup;

  src/app/features/login/login.component.ts


